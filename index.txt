import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'TPX Studio | Web Design & Development Studio',
  description: 'Creative web agency crafting modern, high-performance websites.',
  metadataBase: new URL('https://tpxstudio.vercel.app'),
  icons: {
    icon: '/icon.png',
    shortcut: '/icon.png',
    apple: '/icon.png',
  },
  openGraph: {
    images: ['/logo.png'],
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  // Schema.org Structured Data for Google Search Logo
  const jsonLd = {
    '@context': 'https://schema.org',
    '@type': 'Organization',
    name: 'TPX Studio',
    url: 'https://tpxstudio.vercel.app',
    logo: 'https://tpxstudio.vercel.app/logo.png',
  }

  return (
    <html lang="en">
      <head>
        <script
          type="application/ld+json"
          dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}
        />
      </head>
      <body>{children}</body>
    </html>
  )
}